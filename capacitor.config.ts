import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.greenhome.timbratura',
  appName: 'timbratura-presenze',
  webDir: 'build'
};

export default config;
